# Dependencias para los scripts locales (corren en tu Mac/Windows, no en Docker)
pynput>=1.7
sounddevice>=0.4
scipy>=1.11
numpy>=1.24
httpx>=0.27
plyer>=2.1      # notificaciones en Windows/Linux (en macOS usa osascript)
